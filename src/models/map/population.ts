export interface ICreateMultipleExtrusions {
  center: [number, number];
  totalPopulation: number;
  maxHeight: number;
  cellSize: number;
  name: string;
  gap: number;
  minPopulation: number;
  maxPopulation: number;
  newMax: number;
  newMin: number;
}
